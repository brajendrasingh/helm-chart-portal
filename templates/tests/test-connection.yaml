apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-chart-portal.fullname" . }}-test-connection"
  labels:
    {{- include "helm-chart-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-chart-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
